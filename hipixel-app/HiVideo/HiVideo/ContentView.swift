// ContentView.swift — HiVideo
// 保留为 Xcode 模板的原始文件，实际根视图在 RootView.swift
// 此文件仅用于 Xcode 预览兼容性

import SwiftUI

struct ContentView: View {
    var body: some View {
        RootView(showPlayer: .constant(false))
    }
}

#Preview {
    ContentView()
}
